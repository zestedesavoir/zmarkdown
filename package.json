{
  "devDependencies": {
    "lerna": "^2.0.0-rc.5",
    "babel-preset-es2015": "^6.24.1",
    "eslint": "^3.19.0"
  },
  "scripts": {
    "test": "lerna exec npm run test",
    "coverage": "lerna exec npm run coverage",
    "coverage:combine": "nyc report --include \"./packages/**/coverage/coverage.lcov\" lcovonly text-summary"
  }
}
